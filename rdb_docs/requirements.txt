# Pinned to the combination the site is actually built and verified against
# locally (0 warnings). sphinx_rtd_theme 1.x would drag in Sphinx < 6, which
# this site has never been built with.
sphinx==9.1.0
sphinx_rtd_theme==3.1.0
