# Prerequisites
*.d
*.log
*.nc
*.png
build/*
build_validate/*
# CMake build trees (gfortran CPU, NVHPC GPU, per-toolchain scratch builds)
another_build/
*_build/
# Build-dir NAMING RULE (2026-07-27, post git-store corruption): new build
# trees must be named `build_<toolchain>` (build_serial, build_cc90, ...).
# `build*/` + `*_build/` together cover both the rule and the legacy names;
# an unignored build tree in a `git add -A` is how the object store got
# poisoned.  non_mpi/ was the other stray.
build*/
non_mpi/
nvhpc_*/
*.nsys-rep
*.sqlite
*output*/*
*.dat
*.csv
# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Linker files
*.ilk

# Debugger Files
*.pdb

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# debug information files
*.dwo

# Python
__pycache__/
*.pyc
*.egg-info/

# Jupyter — chapters are tracked as Jupytext-paired .md files; the .ipynb
# renders and checkpoint dirs are regenerated locally on demand
# (jupytext --sync */chapter.md).
*.ipynb
.ipynb_checkpoints/

# Mac os
.DS_Store

# Claude
.claude/

# Local/throwaway artifacts (prototypes, scratch scripts, generated data,
# logs, profiler dumps) — see CLAUDE.md "Local development conventions"
tmp_local_artifacts/

test*.nml
*.gif
_deps/*
CMakeFiles/*
benchmarks/CMakeFiles/*
tests/CMakeFiles/*
tests/generated/*
local_docs/*
examples/tasman_validation/make_bathy.py

# --- Local scratch (keeps `git add -A` from sweeping it into a commit) ---
# Deliberately conservative: only UNAMBIGUOUS throwaway.  Pending work that
# might still be committed (docs/ plans, examples/, validation_examples/,
# tools/) is intentionally NOT ignored -- the no-bulk-adds hook guards those.
out_*/
local_archive/
local_mres/
myenv/
# Site-specific toolchain setup scripts kept on a dev box: bare `module load`
# lines against ONE machine's module tree, so they must never ship.  The
# tooling (validate.sh, tests/regression/run_all.py) sources them only when
# RDB_ON_DEV=1 and they exist, and is a silent no-op in a clean checkout.
/gcc_env.sh
/nvhpc_env.sh
my_output
.vscode/
Testing/
*.mp4
*.npz
# named scratch at the repo root
/a
/a.py
/b
/c
/hepl
/log
/resume
/test_time.txt
/validate_output.txt
/SIM_DETAILS.md
/jorge_todo.md
/rdb_parameter_doc.*
/rdb_coastal_poster.html
